# ED_Mediawatch — backend (X collection slice)
# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Database — SQLite (local-first) now, Postgres/pgvector later (same models)
SQLAlchemy[asyncio]==2.0.36
aiosqlite==0.20.0
# asyncpg==0.30.0          # uncomment to switch to PostgreSQL
# pgvector==0.3.6          # uncomment for embeddings/clustering phase

# Collection (Nitter RSS + French press)
feedparser==6.0.11
aiohttp==3.11.11
python-dateutil==2.9.0.post0
tenacity==9.0.0
trafilatura==2.0.0          # full-text article extraction (press collector)
lxml==5.3.0                 # HTML parsing (Nitter engagement)
cssselect==1.2.0            # CSS selectors for the Nitter HTML parser

# Scheduling & logging
APScheduler==3.11.0
structlog==24.4.0

# LLM (extraction de claims — tier-1 open via OpenAI-compatible, tier-2 Anthropic)
anthropic==0.42.0
openai==1.59.5             # client OpenAI-compatible : Groq / Cerebras / Mistral
cohere==5.13.0             # embeddings multilingues (blocking sémantique des référents)
